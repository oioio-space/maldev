---
last_reviewed: 2026-05-05
reflects_commit: 0a6b52e
---

# Handoff — 2026-05-05 Windows session

> Continues the cross-machine resume series. Resume on any
> PC with `git pull` from `master` and the notes below.

## What landed in this session

22 tags across three themes (v0.32.1 → v0.42.2). Whole-module
build clean as of `0a6b52e`.

### Theme 1 — Backlog grinding (P1.7 closure → P3.5 close → P2.5/P2.13)

Tags shipped (in order):

| Tag | Closes | Surface |
|---|---|---|
| `v0.32.1` | P1.7 row 5 | Backfilled `# Required privileges` + `# Platform` headings on all 97 doc.go pages. |
| `v0.33.0` | (operator UX fix) | `evasion/stealthopen.MultiStealth` — drop-in `*Standard` replacement that captures NTFS Object IDs lazily per path. |
| `v0.35.0` | P3.3 rows 1+2 | `recon/antivm.HypervisorPresent` / `HypervisorVendor` / `HypervisorVendorName` — real CPUID-based VM detection. |
| `v0.35.1` | (/simplify) | Case-insensitivity + alignment fixes from 3-agent review. |
| `v0.36.0` | P3.3 rows 3+4 | `RDTSCDelta` + `LikelyVirtualizedByTiming` + `Hypervisor()` aggregator (parallel session). |
| `v0.37.0` | P2.1 row 3 | `crypto` streaming AEAD (parallel session). |
| `v0.38.0` | P2.5 | `credentials/goldenticket.ValidatePAC` — verifies forged-PAC server + KDC signatures via the same MS-PAC §2.8 dance buildPAC uses (in reverse). |
| `v0.38.1` | P3.5 row 2 | Refresh `evasive-injection.md` with `cet.Marker`/`Wrap` + `inject.ExecuteCallbackBytes` + `preset.Stealth`/`CETOptOut` + MultiStealth-aware variant. |
| `v0.38.2` | P3.5 row 3 | Refresh `full-chain.md` — 7 phases with `Hypervisor()` two-tier bail, masquerade build-step, sleepmask Ekko loop. |
| `v0.38.3` | P3.5 row 4 → closes P3.5 | NEW `dllproxy-side-load.md` end-to-end example. |
| `v0.39.0` | P2.13 row 2 | `pe/cert.Forge` — pure-Go cert chain forge (Leaf → optional Intermediate → self-signed Root → PKCS#7 SignedData → WIN_CERTIFICATE). |

### Theme 2 — `debug/pe` → `github.com/saferwall/pe` migration (commits 1-5/N)

| Tag | Site | Outcome |
|---|---|---|
| `v0.40.0` | `pe/parse` (pivot) | Internal swap to saferwall + new `Authentihash` / `ImpHash` / `Anomalies` methods + `Section` wrapper type. |
| `v0.40.1` | `pe/imports` | New saferwall-backed parser; `Import` struct gains `Ordinal` / `ByOrdinal` / `Hint` (debug/pe was lossy). |
| `v0.40.2` | `evasion/unhook` | Drops 90 lines of hand-rolled export-walk. New `pe/parse.ExportRVA` + `DataAtRVA` helpers added in the same commit. |
| `v0.40.3` | `evasion/callstack` | `peTextRange` via `parse.SectionByName`. |
| `v0.40.4` | `credentials/lsassdump` | `eprocess_offset.go` shrinks 390 → 247 LOC. 5 hand-rolled helpers (findExportRVA / sectionForRVA / readU32At / readU16At / readAsciizAt) collapse into 2 calls per discovery flow. |

**Intentionally NOT migrated**: `pe/dllproxy` uses `debug/pe` to *write* PE structs (FileHeader / OptionalHeader32/64 / SectionHeader32 / DataDirectory). saferwall is a parser, not a writer — its constants would replace 1:1 but the structs would not. Documented in the v0.40.2 commit message.

### Theme 3 — saferwall capability exposure (post-migration)

After the migration closed the parser-side cleanup, three more
tags exposed saferwall capabilities maldev hadn't previously
surfaced:

| Tag | Site | Surface |
|---|---|---|
| `v0.41.0` | `pe/parse` | `(*File).RichHeader()` returning `*RichHeader{XORKey, Tools, Raw}` + `RichTool{ProductID, MinorCV, Count, ProductName, VSVersion}`. Strongest single fingerprint of the MSVC linker that built a PE — defenders use it for attribution clustering, operators clone donor bytes onto Go-built implants. |
| `v0.42.0` | `pe/cert` | `(*Certificate).Parse() (*ParsedAuthenticode, error)` decodes the WIN_CERTIFICATE-wrapped PKCS#7 SignedData via secDre4mer/pkcs7. Surfaces signer (Subject / Issuer / Serial / NotBefore / NotAfter / Algorithm) + full embedded chain. Sentinels `ErrCertificateTooSmall` / `ErrCertificateNoSigners`. Closes the Forge ↔ Parse loop end-to-end + unblocks future "Forge with real SpcIndirectDataContent" work. |
| `v0.42.1` | `pe/imports` | `Import.Delay bool` + `ListDelay(pePath)` surface IMAGE_DELAY_IMPORT_DESCRIPTOR entries. Modern Windows binaries (Edge: 153 delay imports, Office, OneDrive, Teams) route the bulk of their dependencies through delay-load — the previous implementation silently dropped them. |
| `v0.42.2` | `pe/cert` + `pe/parse` | `cert.Inspect(pePath)` one-call wrapper (Read + Parse), `(*pe/parse.File).Overlay()` + `OverlayOffset()` for "appended payload" / signature-blob detection. |

## What's open in the backlog

After this session, the remaining open rows in
[`backlog-2026-04-29.md`](backlog-2026-04-29.md) are:

**Post-deploy verifications** (need live mdBook):
- P1.1 collapsed sidebar
- P1.2 Package-link 404 sweep
- P1.9 dead-link checker

**Code work** (no VM blockers):
- P2.6 `SpoofCallHWBP` — Fortra-style HW-breakpoint callstack variant. ~400-600 LOC, complex — VEH + DR0-3 + single-stepping. The existing `SpoofCall` is marked EXPERIMENTAL with documented crash, so the HWBP variant should be authored carefully + gated behind `MALDEV_SPOOFCALLHWBP_E2E=1` until the asm is verified.
- P2.12 (3 rows) — masquerade preset binaries (notepad, msedge, AcroRd32, WindowsTerminal, OneDrive, Météo, Office). Repetitive `.syso` generation via `pe/masquerade/internal/gen` — may need to extend the generator to accept non-System32 paths first.

**Code work blocked on a VM / harness** (defer):
- P2.9 — phantomdll API refactor (breaking change) + E2E
- P2.11 — RTCore64 E2E (HVCI-off VM)
- P2.23 — BOF Loader `.o` artefacts (mingw cross-compile)

**Research-only** (skip until needed):
- P2.2 / P2.3 / P2.4 last items — research notes

**P3 ideas** (defer):
- P3.1 — Custom packer (4 design rows)
- P3.2 — Red Pill family (5 asm rows)
- P3.4 — VMware I/O port (Ring 0)

## Recommended next moves

In priority order:

1. **P2.6 SpoofCallHWBP** — biggest backlog row left. Fortra-style HW-breakpoint callstack variant. ~400-600 LOC + tests. Risk: existing `SpoofCall` is EXPERIMENTAL with a documented crash path; the HWBP variant adds VEH + DR0-3 + single-stepping complexity. Author with `MALDEV_SPOOFCALLHWBP_E2E=1` gating until verified on a real target.
2. **Upgrade `cert.Forge` to produce real Authenticode** — now that `(*Certificate).Parse()` shipped (v0.42.0), the missing piece is computing the PE's Authenticode hash + wrapping it in a Microsoft-OID `SpcIndirectDataContent`. saferwall.File.Authentihash() already gives the hash; need to ASN.1-encode the SpcIndirectDataContent and feed it as `Forge.opts.Content`. ~100 LOC. Operators with stolen leaf keys then get signatures that pass `signtool verify`.
3. **P2.12 (3 rows) — masquerade preset binaries** — extend `pe/masquerade/internal/gen` to accept non-System32 paths, then add presets for msedge, AcroRd32, WindowsTerminal, OneDrive, Office. Repetitive but ships fast.
4. **`(*pe/parse.File).Overlay()` exposure** — saferwall surfaces `OverlayOffset` + `Overlay()` + `NewOverlayReader()`. Useful for "binary has appended payload" detection (common in droppers). ~20 LOC + 1 test.
5. **Recon-side `cert.Read(pePath).Parse()` ergonomic chain** — the current API forces a 2-step `Read` then `Parse`. A `cert.Inspect(pePath) (*ParsedAuthenticode, error)` wrapper would be a one-call operator surface. ~10 LOC.

## Cross-platform reminder

All migrations + new code preserved cross-platform (where the
underlying technique allows). Verified via `GOOS=linux go build`
on `pe/parse`, `pe/imports`. The whole-module build
(`go build $(go list ./... | grep -v scripts/x64dbg-harness)`)
is clean as of `d44957b`.

## Don't lose

- The pre-existing `TestFindReturnGadget_HasUnwindCoverage`
  flake in `evasion/callstack` is environment-specific (depends
  which RET byte `FindReturnGadget` happens to land on first
  in the current ntdll build). Confirmed pre-existing — NOT
  caused by the v0.40.3 migration. Don't chase.
- `scripts/x64dbg-harness/{bsod,phant0m,unhook,unhook_classic,meterpreter_matrix}/main.go`
  reference deleted packages or stale signatures (pre-existing
  drift). Excluded from `go build $(go list ./...)` via the
  `grep -v scripts/x64dbg-harness` filter; they'll need a
  one-shot fix at some point.
