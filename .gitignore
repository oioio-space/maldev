
# Binaires et artefacts de build
# CRITIQUE: ne JAMAIS publier
# Go
# IDE
# OS
# Secrets / certificats
*.bin
*.crt
*.dll
*.dylib
*.exe

# Test fixtures that must ship with the repo (force-include overrides above).
!testutil/clrhost/maldev_clr_test.dll
# Bundled donor cert blobs — research artefacts, intentionally shipped.
!pe/masquerade/donors/blobs/*.bin
*.iml
*.key
*.out
*.p12
*.pem
*.pfx
*.so
*.test
*.xml
.DS_Store
.claude/
.idea/
.vscode/
Thumbs.db
bin/
build/
coverage.txt
dist/
go.work.sum
ignore/
material_theme_project_new.xml
modules.xml
testutil/results/
logs/
scripts/x64dbg-harness/
scripts/vm-install-x64dbg-mcp.ps1
scripts/vm-test-x64dbg-mcp.go
scripts/vm-test/config.local.yaml
scripts/vm-test/kali-env.sh

# Stray ELF test builds in packer testdata (iterative fixture rebuilds
# that aren't the canonical hello_static_pie — *_test suffix, no extension).
pe/packer/runtime/testdata/hello_static_pie_test*

# Stray binaries from `go build ./cmd/...` without -o (Linux names, no
# extension — *.exe already covers the Windows case above).
/memscan-harness
/memscan-harness-ssn
/memscan-mcp
/memscan-server
/packer-suite
/pe-suite
/c2-suite
/cleanup-suite
/collection-suite
/credentials-suite
/injection-evasion
/kernel-byovd
/persistence-admin
/persistence-user
/preset-suite
/test-report
/vmtest
vendor/
workspace.xml
.mcp.json
.worktrees/
